# cissp-redirect

A tiny stable landing page that redirects to the **current** Cloudflare quick-tunnel
URL of the CISSP study app. The tunnel URL changes on every restart; this page is
kept in sync automatically so the bookmark never breaks.

Contains no application content — only a redirect target.
